name: Deploy to Docker Server

on:
  push:
    branches:
      - test

jobs:
  deploy:
    runs-on: self-hosted

    steps:

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Send message to Slack
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"Merhaba, bu bir test mesajıdır!"}' \
          https://hooks.slack.com/services/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
